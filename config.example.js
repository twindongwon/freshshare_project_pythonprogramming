window.FRESHSHARE_CONFIG = {
  // Supabase를 연결하면 GitHub Pages에서도 다른 사용자가 등록글/채팅을 같이 볼 수 있습니다.
  // 1. Supabase 프로젝트 생성
  // 2. supabase_schema.sql 실행
  // 3. 아래 값을 본인 프로젝트 값으로 교체
  // 4. 이 파일을 config.js로 복사
  supabaseUrl: "",
  supabaseAnonKey: ""
};
